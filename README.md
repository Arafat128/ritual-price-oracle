# ritual-price-oracle
 Built an on-chain ETH/USD price oracle on Ritual testnet (chain 1979). The PriceOracle contract │ fetches live data from CoinGecko via Ritual's HTTP precompile (0x0801) and extracts the USD price │ with the JQ precompile (0x0803) in a single transaction.
